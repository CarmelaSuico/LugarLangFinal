package com.usc.lugarlangfinal.models;

public class Trip {
    // These names should match what you save in AssignedDriverConductor
    public String tripId, routeCode, vehicleCode, terminal1, terminal2, driverName, conductorName, departureTime, status, franchise, plateNumber;

    public Trip() {} // Required for Firebase
}
